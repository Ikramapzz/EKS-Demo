apiVersion: v1
kind: Service
metadata:
  name: jenkins-eks-demo
  namespace: demo
  labels:
    app: jenkins-eks-demo
spec:
  type: LoadBalancer
  selector:
    app: jenkins-eks-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
