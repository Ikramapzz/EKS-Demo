{
  "name": "jenkins-eks-demo",
  "version": "1.0.0",
  "description": "Simple Express API demoing Jenkins CI/CD to EKS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --ci --coverage"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
